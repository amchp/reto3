version: '3'

services:
  lb:
    image: httpd:2.4
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./httpd.conf:/usr/local/apache2/conf/httpd.conf:ro
      - ./ssl:/usr/local/apache2/ssl:ro

